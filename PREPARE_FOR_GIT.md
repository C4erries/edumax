# Подготовка к загрузке в репозиторий

## ✅ Выполнено:

1. Создан `.gitignore` в корне проекта
2. Создан `.gitignore` в `backend/`
3. Обновлен email в README.md

## 📋 Перед коммитом проверьте:

### 1. Убедитесь, что база данных не попадет в репозиторий:
```bash
# Проверьте, что .gitignore работает
git status
# Не должно быть файлов *.db, *.sqlite, app.db
```

### 2. Проверьте, что виртуальное окружение исключено:
```bash
# .venv/ не должен попасть в репозиторий
```

### 3. Проверьте чувствительные данные:
- ✅ `SECRET_KEY` в `config.py` - дефолтное значение "supersecret" (нормально, пользователь установит свой через .env)
- ✅ `.env` файлы исключены через .gitignore
- ✅ Пароли библиотек хранятся в БД, не в коде

### 4. Структура проекта:
```
HackathonMaxNov/
├── .gitignore          ✅ Создан
├── backend/
│   ├── .gitignore     ✅ Создан
│   ├── app/
│   ├── requirements.txt
│   ├── README.md       ✅ Обновлен
│   └── ...
└── Техническое задание...
```

## 🚀 Команды для первого коммита:

```bash
cd /Users/polevskyy/Documents/Projects/HackathonMaxNov

# Инициализация git (если еще не сделано)
git init

# Добавление всех файлов (с учетом .gitignore)
git add .

# Проверка, что добавлено правильно
git status

# Первый коммит
git commit -m "Initial commit: EDU MAX Backend API v3.0.0"

# Добавление remote (замените на ваш URL)
git remote add origin <your-repo-url>

# Push
git push -u origin main
```

## ⚠️ Важно:

1. **База данных** (`app.db`) не должна попасть в репозиторий - она в .gitignore
2. **Виртуальное окружение** (`.venv/`) не должно попасть - оно в .gitignore
3. **Статические файлы** (загруженные документы) не должны попасть - `static/` в .gitignore
4. **`.env` файлы** не должны попасть - они в .gitignore

## 📝 Что будет в репозитории:

✅ Весь исходный код
✅ requirements.txt
✅ README.md и документация
✅ Скрипты инициализации (init_db.sh, seed_*.py)
✅ Docker файлы (Dockerfile, docker-compose.yml)
✅ Конфигурационные файлы

❌ База данных
❌ Виртуальное окружение
❌ Загруженные файлы (static/)
❌ .env файлы
❌ Кэш Python (__pycache__)
