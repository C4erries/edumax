# Система управления образовательным процессом

Backend часть системы управления образовательным процессом для учебных заведений.

## Описание

Система предназначена для управления образовательным процессом в университетах и включает в себя:

- Верификацию пользователей (студентов, преподавателей, сотрудников)
- Управление структурой учебного заведения (ВУЗы, факультеты, кафедры, группы)
- Управление расписанием занятий
- Отслеживание изменений расписания
- Систему запросов и согласований

## Технологии

- **FastAPI** - современный веб-фреймворк для Python
- **SQLAlchemy** - ORM для работы с базой данных
- **SQLite/PostgreSQL** - база данных (SQLite для разработки, PostgreSQL для продакшена)
- **Pydantic** - валидация данных
- **Uvicorn** - ASGI сервер

## Структура проекта

```
app/
├── api/              # API endpoints
│   └── v1/
│       └── endpoints/
│           ├── module1.py    # Верификация пользователя
│           └── module2.py    # Управление пользователями и расписанием
├── core/             # Конфигурация и настройки
├── db/               # Работа с базой данных
├── models/           # SQLAlchemy модели
├── schemas/          # Pydantic схемы
└── services/         # Бизнес-логика
```

## 🐳 Развертывание с Docker (Рекомендуется)

Самый простой способ развернуть проект - использовать Docker Compose.

**База данных:** Проект использует SQLite для простоты развертывания. База данных сохраняется в Docker volume.

### Быстрый старт

```bash
# 1. Настройте окружение
cp .env.example .env
# Отредактируйте .env при необходимости

# 2. Запустите контейнер
docker-compose up -d

# 3. Инициализируйте базу данных
docker-compose exec backend bash /app/init_db.sh

# 4. Проверьте работу
curl http://localhost:8000/health
```

Откройте в браузере:
- **Swagger UI**: http://localhost:8000/docs
- **Health check**: http://localhost:8000/health

Подробная документация: [QUICK_START.md](QUICK_START.md) и [DEPLOYMENT.md](DEPLOYMENT.md)

## Подготовка окружения (локальная разработка)

### 1. Создание виртуального окружения

```bash
python3 -m venv .venv
source .venv/bin/activate  # На Windows: .venv\Scripts\activate
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

## Настройка

Создайте файл `.env` в корне проекта (опционально):

```env
DATABASE_URL=sqlite:///./app.db
SECRET_KEY=your-secret-key-here
API_V1_PREFIX=/api/v1
```

По умолчанию используется SQLite база данных в файле `app.db`.

## Запуск приложения

```bash
uvicorn app.main:app --reload
```

Приложение будет доступно по адресу:
- **API**: `http://127.0.0.1:8000`
- **Swagger UI**: `http://127.0.0.1:8000/docs`
- **ReDoc**: `http://127.0.0.1:8000/redoc`
- **Health Check**: `http://127.0.0.1:8000/health`

## База данных

При первом запуске таблицы будут созданы автоматически в файле `app.db` (SQLite).

Для использования PostgreSQL измените `DATABASE_URL` в `.env`:
```env
DATABASE_URL=postgresql://user:password@localhost/dbname
```

### Заполнение начальными данными

Для заполнения базы данных начальными данными выполните два скрипта:

#### 1. Структура учебных заведений

```bash
python seed_data.py
```

Скрипт добавит:
- 15 крупных российских университетов
- По 3 факультета на каждый университет
- По 2 кафедры на каждый факультет
- По 2 студенческие группы на каждый факультет

#### 2. Тестовые студенты и преподаватели (моковая база деканата)

```bash
python seed_students.py
```

Скрипт добавит:
- 15 тестовых студентов (для верификации при регистрации)
- 8 тестовых преподавателей
- 4 тестовых сотрудника деканата

#### 3. Моковое расписание

```bash
python seed_schedule.py
```

Скрипт добавит:
- 8 временных слотов (пар) с расписанием времени
- 32 предмета (для разных типов факультетов)
- 13 аудиторий (в разных корпусах)
- Расписание на неделю для всех групп (по 3-4 пары в день)

#### 4. Тематические события

```bash
python seed_events.py
```

Скрипт добавит:
- **20+ тематических событий** для университета:
  - Бизнес-куб и стартап-выходные
  - День первокурсника и ориентационные сессии
  - Встречи с экспертами (IT, инвестиции, карьера)
  - Научные конференции
  - Хакатоны (разработка, машинное обучение)
  - Культурные мероприятия (концерты, выставки)
  - Мастер-классы (публичные выступления, мобильная разработка)
  - Спортивные мероприятия
  - Языковые клубы и международные события
- События распределены по датам на месяц вперед
- Смешанные форматы: бесплатные и платные, онлайн и офлайн

#### 5. Элективные курсы

```bash
python seed_electives.py
```

Скрипт добавит:
- **22+ элективных курса** по различным направлениям:
  - **Программирование и IT**: Python, веб-разработка, машинное обучение, мобильная разработка, кибербезопасность
  - **Бизнес**: Предпринимательство, цифровой маркетинг, финансовая грамотность
  - **Дизайн**: Графический дизайн, UI/UX, фотография и видеомонтаж
  - **Языки**: Деловой английский, разговорный английский, китайский язык
  - **Наука**: Научное письмо, статистика и анализ данных
  - **Личностное развитие**: Ораторское искусство, тайм-менеджмент, эмоциональный интеллект
  - **Специализированные**: Блокчейн, 3D-моделирование, робототехника, AI в медицине
- Каждый курс привязан к преподавателю
- Указаны расписание, количество мест и кредиты

#### 6. Доступ к электронной библиотеке

```bash
python seed_library.py
```

Скрипт добавит:
- **Доступ к библиотеке Юрайт** для всех университетов в базе данных
- Для каждого университета создается уникальный логин и пароль
- URL портала: `https://urait.ru/`
- Пошаговая инструкция по доступу к платформе
- Доступ включает:
  - Более 10 000 учебников
  - Более 5 000 курсов
  - Тесты и задания
  - Медиаматериалы (видео и аудио)
  - Мобильное приложение

**Тестовые данные для регистрации:**

**Студенты:**
- Иванов Иван Иванович, билет: `STU001`, город: Москва
- Петров Петр Петрович, билет: `STU002`, город: Москва
- Сидоров Сидор Сидорович, билет: `STU003`, город: Москва

**Преподаватели:**
- Профессоров Александр Иванович, табельный: `TCH001`, город: Москва
- Доцентов Сергей Петрович, табельный: `TCH002`, город: Москва

**Сотрудники:**
- Деканов Иван Петрович, табельный: `STF001`, город: Москва
- Секретарев Мария Сергеевна, табельный: `STF002`, город: Москва

**Примечание:** Оба скрипта можно запускать несколько раз - они проверяют существование данных и не создают дубликаты.

## API Endpoints

### Авторизация и регистрация

- `POST /api/v1/auth/register` - Самостоятельная регистрация пользователя (с автоматической верификацией)
  - **Важно:** Требует `max_id` - ID пользователя из мессенджера MAX (передается ботом)
- `POST /api/v1/auth/verify` - Верификация пользователя (проверка существования в базе деканата)
- `POST /api/v1/auth/login` - Получение JWT токена доступа по user_id
- `GET /api/v1/auth/login-by-max-id?max_id={max_id}` - Получение JWT токена доступа по max_id (для тестирования и работы бота)

### Университеты

- `GET /api/v1/universities` - Получить список всех университетов
- `GET /api/v1/universities/{university_id}/faculties` - Получить список факультетов университета
- `GET /api/v1/universities/{university_id}/faculties/{faculty_id}/groups` - Получить список групп факультета
- `GET /api/v1/universities/{university_id}/faculties/{faculty_id}/kafedras` - Получить список кафедр факультета

### Управление пользователями

- `POST /api/v1/students/add` - Добавление нового студента
- `POST /api/v1/teachers/add` - Добавление нового преподавателя

### Управление расписанием

- `GET /api/v1/schedule` - Получение расписания для группы или преподавателя
- `PATCH /api/v1/schedule/patch` - Применение патчей к расписанию (создание, обновление, удаление занятий)
- `GET /api/v1/schedule/changelog` - Получение журнала изменений расписания

### Служебные

- `GET /health` - Проверка состояния сервиса

## Модели данных

### Основные сущности

- **University** - ВУЗ
- **Faculty** - Факультет
- **Kafedra** - Кафедра
- **StudentGroup** - Учебная группа
- **User** - Пользователь (студент, преподаватель, сотрудник, администратор)
- **Student** - Студент
- **Teacher** - Преподаватель
- **Staff** - Сотрудник деканата/ректората
- **Lesson** - Занятие
- **Subject** - Предмет
- **Room** - Аудитория
- **Timeslot** - Временной слот (пара)
- **ScheduleMeta** - Метаданные расписания
- **ScheduleChangelog** - Журнал изменений расписания
- **Request** - Запрос
- **ApprovalRoad** - Путь согласования

## Примеры использования

### Регистрация студента (с тестовыми данными)

**Важно:** 
- Регистрация происходит через бота в мессенджере MAX
- Бот передает `max_id` - ID пользователя из мессенджера MAX
- Сначала получите ID университета, факультета и группы через endpoints `/universities`

```bash
# 1. Получить список университетов
curl "http://127.0.0.1:8000/api/v1/universities"

# 2. Получить факультеты университета (замените {university_id})
curl "http://127.0.0.1:8000/api/v1/universities/{university_id}/faculties"

# 3. Получить группы факультета (замените {university_id} и {faculty_id})
curl "http://127.0.0.1:8000/api/v1/universities/{university_id}/faculties/{faculty_id}/groups"

# 4. Зарегистрировать студента (используйте тестовые данные из seed_students.py)
# max_id - это ID пользователя из мессенджера MAX (для тестирования можно использовать любое число, например 12345)
curl -X POST "http://127.0.0.1:8000/api/v1/auth/register" \
  -H "Content-Type: application/json" \
  -d '{
    "max_id": 12345,
    "role": "student",
    "full_name": "Иванов Иван Иванович",
    "city": "Москва",
    "university_id": "uuid-университета",
    "faculty_id": "uuid-факультета",
    "group_id": "uuid-группы",
    "student_card": "STU001"
  }'
```

**Пример с тестовыми данными:**
- `max_id`: ID пользователя из мессенджера MAX (для тестирования можно использовать любое число)
- ФИО должно совпадать с данными в базе деканата: `Иванов Иван Иванович`
- Номер студенческого билета: `STU001`
- Город: `Москва`
- После регистрации автоматически выполнится верификация, и если данные совпадают с базой деканата, регистрация будет успешной
- Если пользователь найден в БД деканата, его `max_id` будет обновлен

### Получение токена по max_id (для тестирования)

```bash
# После регистрации можно получить токен по max_id
curl "http://127.0.0.1:8000/api/v1/auth/login-by-max-id?max_id=12345"
```

### Получение списка университетов

```bash
curl "http://127.0.0.1:8000/api/v1/universities"
```

### Получение факультетов университета

```bash
curl "http://127.0.0.1:8000/api/v1/universities/{university_id}/faculties"
```

### Верификация студента

```bash
curl -X POST "http://127.0.0.1:8000/api/v1/auth/verify" \
  -H "Content-Type: application/json" \
  -d '{
    "role": "student",
    "full_name": "Иванов Иван Иванович",
    "student_card": "12345",
    "city": "Москва"
  }'
```

### Добавление студента

```bash
curl -X POST "http://127.0.0.1:8000/api/v1/students/add" \
  -H "Content-Type: application/json" \
  -d '{
    "full_name": "Петров Петр Петрович",
    "student_card": "67890",
    "faculty_id": "uuid-факультета",
    "group_id": "uuid-группы",
    "city": "Москва"
  }'
```

### Получение расписания

```bash
curl "http://127.0.0.1:8000/api/v1/schedule?group_id=uuid-группы"
```

### Применение патчей к расписанию

```bash
curl -X PATCH "http://127.0.0.1:8000/api/v1/schedule/patch?group_id=uuid-группы" \
  -H "Content-Type: application/json" \
  -d '[
    {
      "action": "create",
      "data": {
        "teacher_user_id": "uuid-преподавателя",
        "room_id": "uuid-аудитории",
        "subject_id": "uuid-предмета",
        "pair_no": 1,
        "group_ids": ["uuid-группы"]
      }
    }
  ]'
```

## Разработка

### Структура кода

- **Models** (`app/models/`) - SQLAlchemy модели базы данных
- **Schemas** (`app/schemas/`) - Pydantic схемы для валидации и сериализации
- **Services** (`app/services/`) - Бизнес-логика приложения
- **Endpoints** (`app/api/v1/endpoints/`) - API endpoints

### Добавление новых endpoints

1. Создайте функцию в соответствующем сервисе (`app/services/`)
2. Создайте Pydantic схемы в `app/schemas/`
3. Создайте endpoint в `app/api/v1/endpoints/`
4. Зарегистрируйте роутер в `app/api/v1/router.py`

## Тестирование

Для тестирования API используйте Swagger UI по адресу `http://127.0.0.1:8000/docs`.

## Лицензия

MIT

## Контакты

- Email: insentodesu@icloud.com
