# 🚀 Быстрый старт с Docker (SQLite)

## 1. Настройка окружения

```bash
cd backend
cp .env.example .env
# При необходимости отредактируйте .env (особенно SECRET_KEY для продакшена)
```

## 2. Запуск

```bash
# Запустить контейнер
docker-compose up -d

# Посмотреть логи
docker-compose logs -f
```

## 3. Инициализация базы данных

```bash
# Вариант 1: Использовать скрипт (рекомендуется)
docker-compose exec backend bash /app/init_db.sh

# Вариант 2: Вручную
docker-compose exec backend bash
# Внутри контейнера:
python -c "from app.db.base import Base; from app.db.session import engine; Base.metadata.create_all(bind=engine)"
python seed_data.py
python seed_students.py
python seed_schedule.py
python seed_events.py
python seed_electives.py
exit
```

**Скрипт автоматически загрузит:**
- Университеты, факультеты, группы
- Тестовых студентов и преподавателей
- Расписание занятий
- **20+ тематических событий** (бизнес-куб, встречи с экспертами, хакатоны, конференции и др.)
- **22+ элективных курса** (программирование, дизайн, языки, бизнес, личностное развитие и др.)

## 4. Проверка

Откройте в браузере:
- **Swagger UI**: http://localhost:8000/docs
- **Health check**: http://localhost:8000/health

## 5. Остановка

```bash
docker-compose down
```

## Полезные команды

```bash
# Пересобрать после изменений
docker-compose up -d --build

# Перезапустить
docker-compose restart backend

# Посмотреть статус
docker-compose ps

# Очистить все (включая БД!)
docker-compose down -v
```

## База данных

База данных SQLite хранится в файле `app.db` в корне проекта. 
Данные сохраняются в Docker volume `db_data`, поэтому не потеряются при перезапуске.

### Бэкап БД

```bash
# Создать бэкап
docker cp hackathon_backend:/app/app.db ./backup_$(date +%Y%m%d_%H%M%S).db

# Восстановить
docker cp ./backup.db hackathon_backend:/app/app.db
```

## Полная документация

См. [DEPLOYMENT.md](DEPLOYMENT.md) для детальной информации.
